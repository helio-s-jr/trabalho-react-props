import React from "react";

export default function Footer(props){
    return(
        <Footer>
            <h2>Não consegui exportar a imagem, mas vou pesquisar e estudar mais</h2>
        </Footer>
    );
}