import React from "react";

export default function Header(props){
    return (
        <Header>
            <h1>Minha página de trabalho Props</h1>
            <p>{props.subt}</p>
        </Header>
    )
}